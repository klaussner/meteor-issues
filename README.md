Run this app with `meteor`, then stop it and run it with a different port. For example: `meteor --port 4321`.
After changing the port, the error `MongoError: not master and slaveOk=false` is thrown on startup.
